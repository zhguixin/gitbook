### 简介

HTTP协议是应用层的协议，该协议定义了**对数据包装和解析**的标准。服务器端、客户端按照协议规定可以正常的发送和解析数据。

### 请求报文



### Cookie

HTTP的连接是无状态的，意思就是每一次请求建立连接，请求完成后就把连接断开了，这么做也是为了减少服务器的压力。这也导致了一个问题，

响应报文，有一个`Set-Cookie`首部字段信息；用户在下次的请求报文中，加入这个`Set-Cookie` 的值。服务器就可以判断是那个用户的请求了。